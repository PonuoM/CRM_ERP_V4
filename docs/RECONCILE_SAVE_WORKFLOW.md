# Flow การทำงานของ Reconcile Save

## 📋 สรุปง่ายๆ

เมื่อกด "บันทึกการตรวจสอบ" มันจะ:

### 1. สร้าง Batch (ชุดการตรวจสอบ)
- INSERT 1 record ใน `statement_reconcile_batches`
- ได้เลขที่เอกสาร เช่น `KBANK-20250201-001`

### 2. สำหรับแต่ละ Statement ที่เลือก:

#### 2.1 ตรวจสอบ (Validation)
- ตรวจสอบว่า Statement มีอยู่จริง
- ตรวจสอบว่า Order มีอยู่จริง
- ตรวจสอบว่าไม่ reconcile ซ้ำ
- ตรวจสอบว่าจำนวนเงินไม่เกินยอด Order

#### 2.2 บันทึก Reconciliation Log
- INSERT 1 record ใน `statement_reconcile_logs`
- เก็บ: Statement ID, Order ID, จำนวนเงิน

#### 2.3 อัพเดท Order
- UPDATE `orders` table
- เปลี่ยน:
  - `amount_paid` = ยอดที่จ่ายแล้ว
  - `payment_status` = "Approved" หรือ "PreApproved"
  - `order_status` = อาจเปลี่ยนเป็น "Preparing"

### 3. Commit Transaction
- ถ้าสำเร็จ → บันทึกถาวร
- ถ้า error → ยกเลิกทั้งหมด

## 🎯 ผลลัพธ์

หลังจากบันทึกสำเร็จ:
- ✅ มี `statement_reconcile_batches` 1 record
- ✅ มี `statement_reconcile_logs` ตามจำนวน statements ที่เลือก
- ✅ Orders ถูกอัพเดทสถานะการชำระเงิน

## ⚠️ ข้อควรระวัง

1. **Transaction** - ถ้ามี error เกิดขึ้น → rollback ทั้งหมด
2. **Validation** - ต้องตรวจสอบทุกขั้นตอน
3. **Collation** - ต้องมี collation ตรงกัน

## 🔍 วิธีตรวจสอบปัญหา

ถ้ายังมี error:
1. ดู error log ของ PHP
2. ตรวจสอบว่า error เกิดจาก INSERT หรือ UPDATE
3. ตรวจสอบ collation ของ columns ที่เกี่ยวข้อง
4. ตรวจสอบ foreign key constraints


