# 🧺 Basket Campaign V2 - Implementation Plan (Updated)

## สรุปภาพรวม

ระบบ Basket Campaign V2 จะประกอบด้วย **3 หน้าหลัก**:

| หน้า | สถานะ | รายละเอียด |
|------|-------|------------|
| **1. Dashboard V2** | ✅ ทำแล้ว (ต้องแก้ถัง) | หน้าแสดงลูกค้าตามถังต่างๆ สำหรับ Telesale |
| **2. หน้าแจกแบบใหม่** | ❌ ยังไม่ทำ | หน้าแจกลูกค้าแบบสับ |
| **3. ตั้งค่ากฎถัง** | ❌ ยังไม่ทำ | หน้าตั้งค่ากฎต่างๆ ของถัง (เหมือน Order Tab Settings) |

---

## 📊 นิยามถังใหม่ (Updated)

### หน้า Dashboard V2 (ถังที่ Telesale เห็น)

| ถัง | ชื่อ | เงื่อนไข |
|-----|-----|----------|
| 1 | **Upsell** | Order ที่สร้างวันนี้ (same-day) โดยทีม Upsell 2-3 คน |
| 2 | **ลูกค้าใหม่** | ซื้อครั้งแรกใน 30 วันที่ผ่านมา จากทุกแพลตฟอร์ม |
| 3 | **1-2 เดือน** | order_count ≥ 2 และ last_order ≤ 60 วัน |
| 4 | **โอกาสสุดท้าย เดือน 3** | order_count ≥ 2 และ last_order 61-90 วัน |

#### 🟡 รายละเอียด Upsell Basket

**เงื่อนไขเวลา:**
- ต้องดำเนินการภายใน **วันที่สั่งซื้อ** (same-day)
- เมื่อแอดมินปิดการขายได้ → ข้อมูลถูกส่งเข้าตะกร้า Upsell ทันที
- ทีมงาน 2-3 คน standby รับข้อมูลและโทรออกทันทีในวันนั้น

**หลังจบกระบวนการ Upsell:**
- ✅ **Upsell สำเร็จ** → ส่งไปตะกร้า "ลูกค้าใหม่"
- ❌ **Upsell ไม่สำเร็จ** → ส่งไปตะกร้า "ลูกค้าใหม่" เช่นกัน

> สรุป: ใช้แบบ "วันต่อวัน" เพื่อให้เสนอขายต่อเนื่องที่สุดก่อนส่งเข้าระบบ CRM หลัก

### หน้าแจกแบบใหม่ (ถังสำหรับแจกจ่าย)

| ถัง | ชื่อ | เงื่อนไข |
|-----|-----|----------|
| 1 | **หาคนดูแลใหม่** | order_count ≥ 2 และ last_order 91-180 วัน |
| 2 | **รอคนมาจีบให้ติด** | order_count = 1 และ last_order 31-180 วัน |
| 3 | **ถังกลาง 6-12 เดือน** | last_order 181-365 วัน (ไม่สนใจ order_count) |
| 4 | **ถังกลาง 1-3 ปี** | last_order 366-1095 วัน (ไม่สนใจ order_count) |
| 5 | **ถังโบราณ เก่าเก็บ** | last_order > 1095 วัน (มากกว่า 3 ปี) |

---

## ⚠️ สิ่งที่ต้องแก้ไขใน Dashboard V2

### ปัญหาที่พบจากเอกสารเดิม

| กฎเดิม | กฎใหม่ (ถูกต้อง) |
|--------|-----------------|
| Upsell = order ใน 24 ชม. | Upsell = order **วันนี้** (same-day) |
| ลูกค้าใหม่ = Ready to Distribute | ลูกค้าใหม่ = ซื้อครั้งแรกใน 30 วัน |
| 1-2 เดือน = last_order ≤ 60 วัน | 1-2 เดือน = order_count ≥ 2 + last_order ≤ 60 วัน |
| 3 เดือน = last_order 61-90 วัน | โอกาสสุดท้าย = order_count ≥ 2 + last_order 61-90 วัน |
| Last Chance = last_order > 90 วัน | ย้ายไป "หน้าแจกแบบใหม่" แทน |

---

## 🏗️ Proposed Changes

### หน้า 1: Dashboard V2 (แก้ไข)

#### [MODIFY] [TelesaleDashboardV2.tsx](file:///c:/AppServ/www/CRM_ERP_V4/pages/TelesaleDashboardV2.tsx)

1. **แก้ไขนิยาม tabConfigs** ให้ตรงกับกฎใหม่:
   - `Upsell`: ลูกค้าที่มี order **วันนี้** (same-day)
   - `ลูกค้าใหม่`: `order_count = 1` + `first_order_date` ภายใน 30 วัน
   - `1-2 เดือน`: `order_count >= 2` + `last_order_date` ≤ 60 วัน
   - `โอกาสสุดท้าย`: `order_count >= 2` + `last_order_date` 61-90 วัน

2. **ลบถัง** ที่ไม่ใช้อีกต่อไป (ย้ายไปหน้าแจกใหม่)

---

### หน้า 2: หน้าแจกแบบใหม่ (สร้างใหม่)

#### [NEW] [CustomerDistributionV2.tsx](file:///c:/AppServ/www/CRM_ERP_V4/pages/CustomerDistributionV2.tsx)

โครงสร้างคล้าย Dashboard V2 แต่มีถังต่างกัน:
- หาคนดูแลใหม่
- รอคนมาจีบให้ติด
- ถังกลาง 6-12 เดือน
- ถังกลาง 1-3 ปี
- ถังโบราณ เก่าเก็บ

ฟีเจอร์เพิ่มเติม:
- เลือก Telesale เพื่อแจกงาน
- กำหนดจำนวนลูกค้าต่อ Telesale
- ปุ่ม Preview และ Execute

---

### หน้า 3: ตั้งค่ากฎถัง (สร้างใหม่)

#### [NEW] [BasketSettingsPage.tsx](file:///c:/AppServ/www/CRM_ERP_V4/pages/BasketSettingsPage.tsx)

อ้างอิง UI จาก `OrderTabSettingsPage.tsx`:
- Sidebar แสดงรายชื่อถังทั้งหมด
- Content Area แสดงเงื่อนไขของถังที่เลือก
- สามารถ Add/Edit/Delete เงื่อนไขได้

ข้อมูลที่ตั้งค่าได้:
- `basket_name` (ชื่อถัง)
- `min_order_count` (จำนวน order ขั้นต่ำ)
- `max_order_count` (จำนวน order สูงสุด, null = ไม่จำกัด)
- `min_days_since_order` (last_order อย่างน้อยกี่วัน)
- `max_days_since_order` (last_order ไม่เกินกี่วัน)
- `days_since_first_order` (สำหรับถังลูกค้าใหม่)
- `is_active` (เปิด/ปิดใช้งาน)
- `target_page` (Dashboard V2 หรือ Distribution Page)

#### [NEW] API Endpoint

```
GET /api/basket_config - ดึงกฎถังทั้งหมด
POST /api/basket_config - สร้างกฎใหม่
PUT /api/basket_config?id=X - แก้ไขกฎ
DELETE /api/basket_config?id=X - ลบกฎ
```

#### [NEW] Database Table

```sql
CREATE TABLE basket_config (
    id INT PRIMARY KEY AUTO_INCREMENT,
    basket_key VARCHAR(50) UNIQUE NOT NULL,
    basket_name VARCHAR(100) NOT NULL,
    min_order_count INT DEFAULT NULL,
    max_order_count INT DEFAULT NULL,
    min_days_since_order INT DEFAULT NULL,
    max_days_since_order INT DEFAULT NULL,
    days_since_first_order INT DEFAULT NULL,
    target_page ENUM('dashboard_v2', 'distribution') NOT NULL,
    display_order INT DEFAULT 0,
    is_active BOOLEAN DEFAULT TRUE,
    company_id INT NOT NULL,
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    updated_at DATETIME ON UPDATE CURRENT_TIMESTAMP
);
```

---

## ✅ สิ่งที่ทำไปแล้ว (Notes)

| รายการ | สถานะ | หมายเหตุ |
|--------|-------|----------|
| Dashboard V2 UI Layout | ✅ เสร็จ | Header, Basket Tabs, Search, Filters |
| ปุ่มนัดหมายใกล้ถึง (กรอง) | ✅ เสร็จ | กดแล้วแสดงเฉพาะลูกค้าที่มีนัดหมาย |
| ปุ่มซ่อนที่โทรแล้ว | ✅ เสร็จ | เลือกจำนวนวันได้ (วันนี้, 1-30 วัน, ทั้งหมด) |
| ตั้งค่าขั้นสูง | ✅ เสร็จ | Region, Tag, Sort |
| เรียงตามวันที่ได้รับ | ✅ เสร็จ | ล่าสุด / เก่าสุด |
| ค้นหากด Enter | ✅ เสร็จ | ไม่ค้นหาทุกตัวอักษร |

---

## 🔄 ลำดับการทำงาน (Recommended)

### Phase 1: แก้ไข Dashboard V2 (Priority: High)
1. แก้ไขนิยามถังใน tabConfigs
2. เพิ่ม order_count logic
3. ลบถังที่ไม่ใช้

### Phase 2: สร้างหน้า Basket Settings (Priority: High)
1. สร้าง Database Table
2. สร้าง API Endpoint
3. สร้าง UI Page (เหมือน OrderTabSettingsPage)
4. เพิ่มเมนูใน Sidebar

### Phase 3: สร้างหน้าแจกแบบใหม่ (Priority: Medium)
1. สร้าง CustomerDistributionV2.tsx
2. Integrate กับ basket_config
3. ฟีเจอร์แจกงาน

---

## Verification Plan

### Manual Testing

1. **Dashboard V2 Basket Logic**
   - เปิดหน้า `http://localhost:5173/beta_test/?page=Dashboard+V2`
   - ตรวจสอบว่าลูกค้าแสดงในถังที่ถูกต้องตามเงื่อนไข
   - ทดสอบการเปลี่ยน Tab

2. **Basket Settings Page**
   - เปิดหน้าตั้งค่า
   - เพิ่ม/แก้ไข/ลบเงื่อนไขถัง
   - กดบันทึกและตรวจสอบใน database

3. **Distribution Page**
   - เลือก Telesale และแจกงาน
   - ตรวจสอบว่าลูกค้าถูก Assign ถูกต้อง

### ให้ User ช่วยยืนยัน
- ข้อมูลลูกค้าตัวอย่างในแต่ละถังถูกต้องหรือไม่
- UI ใช้งานง่ายหรือไม่

---

## ✅ คำตอบจาก User (Confirmed)

1. **ถัง Upsell** → ใช้เงื่อนไข **วันต่อวัน (same-day)** ไม่ใช่ 24 ชม.
2. **หน้าแจกแบบใหม่** → ต้องการแจก **Manual**
3. **ลำดับความสำคัญ**:
   1. ✅ แก้ Dashboard V2 ก่อน
   2. สร้างหน้า Settings
   3. สร้างหน้าแจกใหม่
